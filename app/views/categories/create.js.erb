<% if @category.errors.any? %>
  displayErrors("<%= j(@category.errors.messages.to_json.html_safe) %>", "category");
<% else %>
  <% flash[:notice] = 'Category was successfully created.' %>
  window.location = "<%= categories_path %>";
<% end %>